# Career Quest

Career Quest — веб-приложение с объяснимым AI-слоем, которое строит траекторию до следующего грейда и рекомендует сотруднику 1–3 релевантные активности на основе профиля, требований роли и истории участия.

Проект создан для кейса Halyk Bank на HackAlem AI. Основной акцент сделан на качестве решения и проверяемом объяснении рекомендации. Геймификация не подменяет рекомендательную логику.

## Коротко о решении

Сотрудник видит не список разрозненных HR-событий, а ответ на три вопроса:

1. Насколько я готов к следующему грейду?
2. Какой шаг даст наибольший эффект сейчас?
3. Почему система рекомендует именно его?

Для HR доступен отдельный агрегированный экран: часто проседающие компетенции, участие в активностях, сотрудники без подходящего шага и приватный список тех, кому может потребоваться помощь.

## Главное отличие

Система не использует правило «рекомендовать самый низкий навык». Каждый кандидат оценивается одновременно по четырём группам факторов:

```text
score = 0.46 × соответствие следующему грейду
      + 0.26 × влияние на карьерную готовность
      + 0.18 × история участия
      + 0.10 × выполнимость активности
```

В интерфейсе видны все факты и вклад каждого фактора. Например, у тестового сотрудника Public Speaking ниже остальных, однако первым рекомендуется System Design: этот навык критичен для Senior, активность заметнее двигает траекторию, а сотрудник трижды пропускал похожие активности по публичным выступлениям.

## Реализованные требования

| Требование ТЗ | Реализация |
|---|---|
| Профиль и карьерная траектория | Роль, грейд, стаж, skill gap, история и процент готовности к следующему грейду |
| 1–3 рекомендации | Многофакторный ранжировщик выбирает до трёх подходящих активностей |
| Объяснение минимум по трём факторам | Для каждого шага показываются четыре фактора и полный score breakdown |
| Обновление прогресса | После завершения активности применяются `gain` и `max_level`, затем пересчитываются навыки и траектория |
| HR-view | Проседающие навыки, завершаемость, сотрудники без следующего шага и приватный watchlist |
| Проверочные профили | Интерфейс принимает дополнительные JSON/CSV-файлы по схеме стартового кита |
| Одна команда запуска | Сервер работает на стандартной библиотеке Python без установки пакетов |
| Разделение сотрудник/HR | Вход, серверные сессии и проверка роли/владельца профиля на каждом API-запросе |

## Запуск

Из папки `career-quest` выполните `python3 server.py` (на Windows также `python server.py`). Откройте `http://127.0.0.1:8000`. Интернет и установка пакетов не нужны.

При запуске сервер печатает логины `employee` и `hr` с отдельными случайными паролями. Пароли не публикуются в браузере. Для постоянных локальных паролей задайте `CQ_EMPLOYEE_PASSWORD` и `CQ_HR_PASSWORD` в окружении до запуска. `employee` привязан к E0028 (стаж 52 месяца); привязку можно изменить через `CQ_EMPLOYEE_ID` на ID встроенного сотрудника. Роль и ID определяет сервер, форма входа не позволяет их назначать.

| Возможность | Сотрудник | HR |
|---|---|---|
| Профиль, история, рекомендации | Только свои | Все профили, чтение |
| Завершение активности, запрос AI, добровольная пауза | Только для себя | Нет |
| HR-сводка и приватный список поддержки | Нет | Да |
| Импорт данных и сброс демо | Нет | Да |

Сессия живёт 8 часов, хранится на сервере и передаётся через `HttpOnly; SameSite=Strict` cookie. Выход отзывает сессию. POST-запросы требуют JSON и CSRF-токен (кроме входа); чужой Origin отклоняется. После пяти неудачных входов с одного IP действует минутное ограничение. Пароли хранятся в памяти в виде PBKDF2-хешей с индивидуальной солью. Статические маршруты разрешают только HTML/CSS/JS интерфейса, исходники и датасеты недоступны.

Это локальное демо, по умолчанию слушающее только `127.0.0.1`. Для внутреннего развёртывания нужны корпоративный доступ, HTTPS на обратном прокси и `CQ_SECURE_COOKIE=1`; встроенный HTTP-сервер не заменяет промышленную инфраструктуру. Сессии, данные и паузы сбрасываются при перезапуске. Публичного обмена данными между сотрудниками нет вообще, поэтому чужая вовлечённость не раскрывается без согласия.

Кнопка «Взять паузу» сохраняет XP и навыки, блокирует завершение активностей до возобновления и исключает сотрудника из HR-списка выпадения. HR видит общее число пауз. Недельная цель добровольна, штрафов за пропуск нет. HR-сигнал объясним: готовность ниже 58% и доля завершений ниже 55%; он предназначен для предложения поддержки.

### Геймификация и наставник

За каждую уникальную завершённую активность начисляется 100 XP, каждые 300 XP открывают новый уровень. Значки выдаются за 1, 3 и 5 активностей. Квест недели — завершить две активности с понедельника по текущую дату сервера. Импортированная история также учитывается; повторные завершения не начисляют XP и не повышают навыки. XP отражает участие, а готовность к грейду рассчитывается отдельно.

Панель «Как улучшить навыки» показывает упражнения, проверяемый результат и следующий шаг для текущих пробелов. План обновляется после завершения активности. Без API доступен явно обозначенный локальный план.

Внутренний режим по умолчанию не отправляет профили внешним сервисам, даже если API-ключ задан. Если организация разрешила внешние AI-советы, явно включите их в PowerShell перед запуском сервера:

```powershell
$env:OPENAI_API_KEY = "your-api-key"
$env:OPENAI_MODEL = "your-available-model-id"
$env:CQ_ALLOW_EXTERNAL_AI = "1"
python server.py
```

Кнопка «Получить совет AI» вызывает `POST /api/coach` через [OpenAI Responses API](https://developers.openai.com/api/docs/guides/migrate-to-responses). Ключ остаётся на сервере. В запрос передаются роль, целевой грейд, навыки и практический план, без имени, ID и исходной истории; используется `store: false`. При ошибке или таймауте остаётся локальный план. Генерация выполняется только по нажатию кнопки и не меняет оценки или XP.

Как и остальные данные демо, прогресс хранится в памяти сервера: переживает обновление страницы, но сбрасывается при перезапуске сервера или сбросе демо. Отметка завершения — самоотчёт сотрудника.

Требуется Python 3.10 или новее.

```bash
cd career-quest
python3 server.py
```

Открыть в браузере: [http://127.0.0.1:8000](http://127.0.0.1:8000).

Другой порт:

```bash
python3 server.py --port 8080
```

Внешние зависимости, API-ключи и доступ к интернету не нужны.

## Сценарий защиты

1. Войти как `employee`: открывается профиль **Аян Серик · Backend Engineer · Middle**.
2. Показать, что самый низкий навык — Public Speaking, но рекомендация №1 развивает System Design.
3. Нажать **Почему этот шаг** и показать четыре фактора решения.
4. Нажать **Отметить выполненным**: System Design растёт, готовность к Senior пересчитывается, рекомендации обновляются.
5. Выйти, войти как `hr` и показать агрегированные skill gaps и участие.
6. Нажать **Загрузить датасет** и добавить проверочный профиль жюри.

Главная демонстрационная фраза:

> Мы не рекомендуем сотруднику очередной курс. Мы показываем, какой шаг приблизит его к следующему грейду, и доказываем это на данных.

## Загрузка данных

Кнопка **Загрузить датасет** принимает один или несколько файлов:

- `employees.json` — профили сотрудников;
- `events.json` — активности и развиваемые навыки;
- `skills.json` — навыки и требования по грейдам;
- `activity_history.csv` — история участия, пропусков и отказов.

Можно загрузить один JSON-объект сотрудника, массив профилей или bundle:

```json
{
  "employees": [
    {
      "employee_id": "E0028",
      "name": "Аян Серик",
      "role": "Backend Engineer",
      "grade": "Middle",
      "tenure_months": 52,
      "skills": {
        "SK_PYTHON": 3,
        "SK_SYSTEM_DESIGN": 2,
        "SK_PUBLIC_SPEAKING": 1
      }
    }
  ],
  "activity_history": []
}
```

При загрузке только профилей и истории используются встроенные справочники навыков и активностей. При загрузке полного стартового кита справочники заменяются загруженными версиями.

## Как принимается решение

Для каждого сотрудника система:

1. Определяет следующий грейд.
2. Получает требуемые уровни навыков.
3. Считает текущие разрывы и общую готовность.
4. Фильтрует активности по роли и реальному эффекту `gain/max_level`.
5. Симулирует состояние после каждой активности.
6. Учитывает завершение, пропуски и отказы от похожих форматов.
7. Ранжирует варианты и возвращает до трёх рекомендаций вместе с фактическим объяснением.

LLM в промышленной версии может переформулировать уже рассчитанные факты на русском или казахском языке, но не меняет score и уровни навыков. Это защищает систему от галлюцинаций и делает решение воспроизводимым.

## Архитектура

```text
JSON / CSV
    │
    ▼
DataStore и проверка схемы
    │
    ├── профиль и требования грейда
    ├── skill gaps
    ├── история участия
    └── правила gain / max_level
    │
    ▼
Explainable Recommendation Engine
    │
    ├── ranking score
    ├── counterfactual simulation
    └── factual explanation
    │
    ├───────────────┐
    ▼               ▼
Employee View     HR View
```

Backend использует `ThreadingHTTPServer`, поэтому параллельные запросы интерфейса не блокируют друг друга. Все расчёты выполняются локально.

## API

| Метод | Маршрут | Назначение |
|---|---|---|
| `GET` | `/api/health` | Проверка готовности сервера |
| `POST` | `/api/login` | Логин и пароль → сессия и CSRF-токен |
| `GET` | `/api/session` | Текущая учётная запись и CSRF-токен |
| `POST` | `/api/logout` | Отзыв сессии |
| `GET` | `/api/employees` | Список профилей, только HR |
| `GET` | `/api/employee?id=E0028` | Траектория и рекомендации сотрудника |
| `GET` | `/api/hr` | Агрегированный HR-срез |
| `POST` | `/api/complete` | Завершение активности и пересчёт прогресса |
| `POST` | `/api/participation` | Своя добровольная пауза: `{"paused": true}` |
| `POST` | `/api/coach` | Свой практический план / AI-совет |
| `POST` | `/api/upload` | Загрузка проверочных профилей и истории |
| `POST` | `/api/reset` | Возврат к демонстрационному набору |

## Тесты

```bash
python3 -m unittest discover -s tests -v
```

Тесты проверяют:

- выбор критичного разрыва вместо минимального навыка;
- изменение навыка и готовности после активности;
- наличие обязательных HR-метрик.

## Структура проекта

```text
career-quest/
├── index.html              # интерфейс сотрудника и HR
├── styles.css              # адаптивный дизайн
├── app.js                  # API, рендеринг, загрузка JSON/CSV
├── engine.py               # рекомендательная логика и демо-данные
├── server.py               # локальный сервер и REST API
├── sample_upload/
│   ├── employees.json      # пример дополнительного профиля
│   └── activity_history.csv
├── tests/
│   └── test_engine.py
└── README.md
```

## Приватность и ограничения

- используются только синтетические данные;
- нет публичного рейтинга сотрудников;
- HR получает агрегаты и рабочий список, а не игровую таблицу лидеров;
- рекомендация не является автоматическим кадровым решением;
- история вовлечённости не показывается другим сотрудникам;
- все числовые выводы можно восстановить из входных данных.

## Потенциал развития

- локализация на казахский язык;
- корпоративный SSO вместо локальных учётных записей;
- интеграция с LMS, HRIS и внутренними вакансиями;
- LLM-объяснение на основе рассчитанных фактов;
- прогноз вероятности завершения активности;
- календарь, мессенджер и менторские механики;
- offline-оценка рекомендаций на исторических данных и A/B-тестирование.
